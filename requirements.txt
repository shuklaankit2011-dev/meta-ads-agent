# Streamlit app (used by Streamlit Cloud)
streamlit>=1.35.0
requests>=2.32.0
python-dotenv>=1.0.0
google-generativeai>=0.7.0
plotly>=5.0.0
pandas>=2.0.0

# Agent scripts (install locally: pip install -r requirements_agent.txt)
# langchain, langgraph, langchain-google-genai → moved to requirements_agent.txt
